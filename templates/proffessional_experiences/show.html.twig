{% extends 'base.html.twig' %}

{% block title %}ProfessionalExperiences{% endblock %}

{% block body %}
    <h1>ProfessionalExperiences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professional_experience.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ professional_experience.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ professional_experience.description }}</td>
            </tr>
            <tr>
                <th>Date_start</th>
                <td>{{ professional_experience.dateStart ? professional_experience.dateStart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_end</th>
                <td>{{ professional_experience.dateEnd ? professional_experience.dateEnd|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proffessional_experiences_index') }}">back to list</a>

    <a href="{{ path('proffessional_experiences_edit', {'id': professional_experience.id}) }}">edit</a>

    {{ include('proffessional_experiences/_delete_form.html.twig') }}
{% endblock %}
